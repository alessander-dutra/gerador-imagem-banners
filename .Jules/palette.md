## 2024-05-23 - Native Validation & Accessibility
**Learning:** Native HTML validation (`required` attribute) provides immediate feedback and accessibility benefits that custom JavaScript validation alone misses. It communicates "required" state to screen readers automatically.
**Action:** Always pair custom validation with native HTML attributes (`required`, `min`, `max`, `pattern`) where possible.
