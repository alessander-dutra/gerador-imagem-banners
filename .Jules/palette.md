## 2024-05-23 - Accessibility Polish for Gallery
**Learning:** Hover-only interactions (`group-hover`) exclude keyboard users unless paired with `group-focus-within`.
**Action:** Always add `group-focus-within` to any container that uses `group-hover` to reveal interactive elements.
